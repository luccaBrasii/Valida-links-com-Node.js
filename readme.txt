1º Os links a serem verificados (formato do arquivo em .MD) devem ficar
no arquivo 'texto.md' na pasta 'arquivos'.

2º o comando a ser executado no terminal é: 'npm run cli:valida'

3º Os resultados serão mostrados no console, verificar o 'status', se for 200 = 'ok',
se não aparecerá o código do erro ou 'link não encontrado'